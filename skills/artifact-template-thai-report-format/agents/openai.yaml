interface:
  display_name: "Thai Report Format"
  short_description: "Create documents with the Thai Report Format template"
  icon_large: "./assets/preview.png"
  default_prompt: "Use $artifact-template-thai-report-format to create a document with this template."
policy:
  allow_implicit_invocation: true
